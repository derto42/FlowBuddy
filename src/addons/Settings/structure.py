# keys
KEY = "key"
TYPE = "type"
SETTING_TYPE = "setting_type"
OPTIONS = "options"
DEFAULT = "default"


# args
UPDATE = "update"

# types
ENTRY = "entry"
SWITCH = "switch"
SPIN = "spin"
DROPDOWN = "dropdown"


STRUCTURE = {
    "UI Group": {
        "UI Scale": {
            KEY: "ui_scale",
            TYPE: float,
            SETTING_TYPE: SPIN,
            OPTIONS: [UPDATE],
        }
    }
}